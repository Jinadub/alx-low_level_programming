#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: Integar to be added
 * @b: Integar to be added
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	int sum;

	sum = (a + b);

	return (sum);
}
