#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Description: program that prints all single digit numbers of base 10
 * Return: 0
 */

int main(void)
{
	int db;

	for (db == '0'; db  <= '9'; db++)
		putchar(db);
	putchar('\n');

	return (0);
}
